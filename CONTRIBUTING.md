# 参与 Dayreed

Dayreed 是个人维护的开源项目，优先做好一个人的记录与回顾体验。欢迎小而完整的修复；较大的功能请先通过 Issue 说明使用场景与取舍。

## 工作方式

1. 搜索已有 Issue；新问题说明期望、实际结果与复现步骤。不要提交原始截图、窗口内容、访问令牌或个人活动数据库。
2. 从 `main` 创建范围明确的分支。维护者使用 `codex/` 前缀；同一功能避免多个任务同时改写共享文件。
3. 运行与改动相关的测试，确保 `swift build` 可通过。UI 改动补充实际截图及系统版本；构建成功不等于视觉验收。
4. 提交和 PR 以中文为主，使用 `feat:`、`fix:`、`refactor:`、`docs:`、`chore:` 等常规前缀；有关联 Issue 时在正文写明。维护者提交沿用自己的 GPG 签名，不要求贡献者提供签名私钥。
5. PR 说明用户可见变化、验证及尚未覆盖的部分。维护者采用 squash 合并，合并后清理功能分支。

无需为简单改动增加审批层或全套测试。涉及数据删除、凭据、更新和发布的改动应把实际影响与恢复方式说明清楚。

## 文件边界

- 版本管理：源码、必要测试、构建工具、公开文档和正式资源。
- 本地保留：`docs/local/` 下的计划、研究、过程截图、日志和临时报告，Agent 工具配置及生成产物。
- 凭据保留在本机 Keychain 或明确配置的受控环境中，不提交到 Git、Issue 或 CI 日志。

尊重他人，讨论围绕问题与证据，不进行人身攻击。维护者按个人时间处理贡献，不承诺固定响应时限。贡献代码默认采用项目的 MIT 许可；第三方内容必须注明原许可。
