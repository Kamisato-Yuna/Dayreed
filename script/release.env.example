# Copy to script/release.env (ignored by Git), then chmod 600 script/release.env.
# Parsed as data: only the two fields below; no shell commands or expansion.
# Use an existing local identity and notarytool Keychain profile.
# Do not put passwords, private keys, or certificate exports in this file.
SIGN_ID='Developer ID Application: Your Name (TEAMID)'
NOTARY_PROFILE='YOUR_EXISTING_NOTARY_PROFILE'
